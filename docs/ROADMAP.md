# TEEMS Solver — Development Roadmap

Working plan following the 2026 refactor (phases 0–4, logged below)
and phase 5's measured performance work. Completed items are never
deleted: phase-5 rows flip to **done** with findings attached, and
phases 0–4 are recorded in the completed-phases log at the end of this
file. `docs/solver-reference.md` describes the *resulting* system.

**Standing rules**
- Every change passes the golden suite (`.audit/verify.sh`, currently 14
  bit-identical gates: 5 method + 3 real-shock + 1 GEMPACK-orientation +
  4 subinterval gates spanning method × ranks × nsubints × inmemory,
  the LU one run in both -inmemory modes) and,
  where performance-relevant, interleaved A/B benchmarks on the
  calibrated rigs. Benchmark only on an idle machine; use paired runs.
- **Goal: full GEMPACK feature parity. Performance is the only veto.**
  Features must be zero-cost when unused. The GEMPACK manual is the
  parity spec; original TABs in `original_tabs/` (GTAP resource
  archives) and `teems_archive/tab_files/` (incl. `GDYNv3.6.tab`,
  `gtapep.tab`) are the test corpus — the teems-adapted TABs in
  `teems-R/inst/models/` are already reduced and cannot reveal gaps.

## Phase 5 — performance/correctness work (**complete** 2026-07-09)

| id | item | status / notes |
|---|---|---|
| 5.5 | Golden-suite widening (real asymmetric shocks: 2D probe, 4D, swap) | **done** — 8 goldens |
| 5.6 | Shock-value ordering aligned to GEMPACK standard (all statement forms); teems-R writer paired (cee6210); golden 9 `gmshk` pins orientation; teems-R custom-shock suite 18/18 | **done** |
| 5.7 | Netcut instrumentation: machine-readable netcut + block-size logging per run | **done** — `<solfiles>.stats.json` written by rank 0 at ordering time (pre-solve): size/method/netcut/border/per-block var+eq counts; excluded from golden manifests |
| 5.8 | `matrix_method = "auto"`: calibration sweeps (size × T/R × ranks), decision rule implemented in teems-R from deploy metadata | **done** — teems-R 3d561a2: deploy writes system_size/n_reg into metadata.rds; auto (now the default) = SBBD if intertemporal (single-rank SBBD also beats LU), else DBBD when n_tasks ≥ 2 and size ≥ 2M eq (or ≥ 1.5M with ≥ 100 regions), else LU. Static crossover data in solver-reference §9 (LU/DBBD ladders at 1.35M–8.3M eq × 33/163 blocks). Residual: NDBBD-vs-SBBD escalation (standing investigation); refine 1.5–2M × <100-region band if real models land there |
| 5.9 | In-memory block-factor handoff: redesign Fortran kernel interface so factors return via memory, not `_vav/_irnv/_keep` files | **done** (code + correctness) — kernels return factors via caller buffers (`KEEP` surfaced with the `M+9N+7` bound; `LA` was always known pre-call, so no grow-loop issue); C holds them resident under `-inmemory` or writes byte-identical legacy files otherwise. All 9 goldens pass in both modes for DBBD and NDBBD. NDBBD resident mode: zero factor traffic (was ~825MB/solve at 202k eq), +22MB RSS/rank, no scratch debris. A/B closed 2026-07-09 (bench-inter-L NDBBD-2, 3 interleaved pairs): wall neutral (medians 385s im0 vs 381s im1; one 350s im1 run suggests mild upside), RSS 1.09→1.84GB/rank (+69%) for resident factors. **Default stays off** — new rationale is RAM cost, not page cache (that rationale died with 5.9); measured on the environment most favorable to im0 (fast local scratch), so slow/network-scratch clusters — NDBBD's habitat — are expected to favor `inmemory=TRUE`, now exposed via `ems_solve()`. Residual interface spills (~65MB/solve) folded into future work |
| 5.10 | Interpreter deep-compaction: interleaved per-dim operand records (`dim_addr`) in `formula_op`; follows the 1416→968B compaction (~1% win). `formula_eval` is 15.6% of wall at scale | **done** (code + correctness) — `dim_addr {ADims, SupSet, SSIndx, leadlag}` replaces the 4 parallel per-dim arrays per operand; the eval loop now reads one 24B record per dim instead of touching 4 arrays 96B apart. `formula_op` 968→848B; `SupSet`/`SSIndx` narrowed offset_t→int. All 9 goldens bit-identical; build warnings 110→102. A/B closed 2026-07-09 (bench-inter-L SBBD-2, eb7f278 vs 2be256f, 3 interleaved pairs): **speed-neutral** — median delta 0.4% inside ~10% session drift; arithmetic agrees (10% of a 16% `formula_eval` share ≈ 1.6% wall, unresolvable by wall-clock A/B). The change stands on its 120B/op footprint cut and layout clarity. NB pre-existing latent bug preserved as-is (goldens are bit-identical gates): the Var2→Var3 dim-field copy self-assigns `SupSet` (`Var3Dims[i].SupSet=Var3Dims[i].SupSet`); ops arrays are calloc'd and slots written once, so operand 3's SupSet is always 0 there — superset indexing silently dropped if a superset-indexed operand lands in that slot (unexercised by the golden corpus). Fix as its own gated change |
| 5.11 | Small batch: verbosity-gated logging **including a full printf overhaul — professional wording (no `!!!!`/`OK012345`/`Hello world`/`RRRRR` debris), each message including errors as informative as possible (say which file/block/rank/phase and what to do about it) without impeding speed (hot-loop prints gated or removed; keep stdout parse contracts teems-R relies on, e.g. `Accurate` lines, or update both sides together)**; `-march` image variants; `ems_solve(inmemory=)` exposure; op-list caching (small-model benefit only); remove dead `isLinux` read path; NDBBD-without-`-regset` diagnostic (currently bare MPI abort) | **done** — printf overhaul complete: `-verbosity {0,1,2}` (default 1; exported as `TEEMS_VERBOSITY` for the Fortran kernels), all 541 C printf sites triaged (debris deleted, per-rank/per-block detail → level 2, errors/warnings never gated and reworded with file/flag/remedy), MA48 duplicate-entry notes silenced below level 2 (`ICNTL(3)=1`), "no supplied values" warned once per array instead of per step (10,822 → 181 lines on golden-re; v0 log 12,358 → 187 lines), `Accurate` contract preserved at all levels; dead `isLinux` paths removed; NDBBD misconfiguration diagnostics added (missing/unmatched `-regset`, missing `-enable_time`; clean collective exit). `ems_solve(inmemory=, verbosity=)` exposed in teems-R (b98b31f; both default NULL = solver decides). `-march` image variants moved to 6.9 (2026-07-09). Op-list caching **measured and dropped** (2026-07-09): on the interactive-scale rig (GTAP-RE 10.5k eq, LU-1 Mmid, 1.16s wall) `formulas_execute` totals 0.04s across 15 per-step calls (3.4% of wall, parse+eval — caching removes only the parse slice); the real small-model cost is `jacobian_fill` (0.71s, 61% of wall), where equation parse interleaves with the mandatory per-step Jacobian refill — out of scope here and restructured wholesale by 6.2 anyway |

**Phase-5 findings** (details in `docs/solver-reference.md` §9):
factorization ≈63% of wall at scale; `formula_eval` ≈16% (compaction of
it measured speed-neutral — the wall is factorization); on small models
the per-step cost is `jacobian_fill`'s equation parse+refill (61% of a
1.16s interactive solve), not formulas (3.4%). `-inmemory` defaults: on
for LU/SBBD/DBBD (+7% SBBD, neutral elsewhere, no debris), off for
NDBBD on RAM-cost grounds (+69% RSS/rank for wall-neutral resident
factors on fast local scratch; slow/network scratch expected to favor
`inmemory=TRUE`, exposed in `ems_solve()`). Speed since refactor: SBBD
~5–8%; LU/DBBD neutral + cleanliness; NDBBD unchanged by design.

## Phase 6 — GEMPACK parity program (performance-vetoed)

**Work order (revised 2026-07-13; previous revision 2026-07-09 is
consumed — 6.1 and 6.5 E1/E2 done):** (1) **6.6(c)+(d)** — needed fix
(published amd64 image can SIGILL on older CPUs) and the designated
golden re-anchor slot: the 6.8 flag removal (`-regset`/`-enable_time`
out of teems-R) rides it, and re-anchors are expensive, so anything
else that breaks bit-identity lands before it or waits for the next
slot. (2) **6.2 condensation** — gates 6.5 E3 (changes what reaches
the border), 6.10 (the probe should measure the condensed system),
6.7's gated experiments (A/Bs against matrices 6.2 is about to
shrink would be invalidated; paper study/shortlisting may proceed
anytime), and softly 6.3(c)/(d) (their cost-benefit gates measure
against the factorization wall 6.2 shrinks). (3) The unblocked
cluster — **6.5 E3 (done 2026-07-17, element-exact), 6.10, 6.7
experiments, 6.3 benchmarked methods** — in any order. Order-free threads, interleave anywhere: 6.3(a)
Euler multi-step (parity-justified, not speed-gated) and the 6.1
residuals → 6.4 (the deferred corpus inventory precedes any large
6.4 push, since it produces the ranking 6.4 is scoped by).

| id | item | notes |
|---|---|---|
| 6.1 | **Immediate language gaps** (scaled back 2026-07-09 from the full parity inventory — that inventory is deferred, not dropped; the specs below are the documented limitations in `teems-manual/model_load.qmd`, whose workaround sections get deleted as each lands): (a) **set expressions** ✅ (2026-07-09, expanded to the full GEMPACK spec 10.1.1.1 per user guidance: UNION/INTERSECT/+/-/backslash over declared sets and quoted single elements, bracketed grouping, left-to-right; implied-SUBSET rules per the manual; '+' disjoint and '-' containment validated) — solver 0f59147 (recursive-descent set_expr_build; legacy pairwise encodings byte-identical, goldens 9/9; .audit/expr-test-kit exercises 3-term/brackets/INTERSECT/quoted forms), teems-R c3bc3bf (R/set_expr.R tokenizer+evaluator shared by parse and finalize; solve-equivalence test vs pairwise baseline passes), manual updated (teems-manual b880e49); (b) **set equality** ✅ (2026-07-09, GEMPACK spec 10.1.2.1: `Set B = A;` copies a previously defined non-intertemporal set and auto-generates the two SUBSET statements in both directions; the 13.3.1 `(Intertemporal)`/`(Non_Intertemporal)` conversion forms abort with a clear error) — solver: set_equality_build in tab_parse.c registers the mutual subsets (the legacy '='-copy branch did elements only; subset_map_build terminates on the cycle by adding self-entries), goldens 9/9, .audit/expr-test-kit TESTD probes formula addressing across the equality pair in both directions (sol.bin bit-identical); teems-R: bare `= A` no longer aborts, equality definitions keep their leading '=' so finalize's expression path (single-term) evaluates the alias, mutual subsets + cycle-safe visited-set transitive closure in tab_set_parse.R, e2e equation-quantifier solve-equivalence test passes; manual workaround section replaced with the supported spec; (c) **IF in Formula/Equation RHS** ✅ (2026-07-10, GEMPACK spec 11.4.5–11.4.7; rewrite-over-surgery: GEMPACK itself translates index-IN-set IFs to conditional sums, and the GTAP authors' hand adaptations are the exact target shapes) — teems-R `R/tab_if_rewrite.R` rewrites top-level additive `IF[cond, expr]` terms at parse time: Formulas → base statement with IF terms dropped + one accumulate formula per term over a narrowed domain (`idx in S` / `idx="elem"` → synthesized intersection set `IFS* = S & range`, whose all-INTERSECT implied subsets keep refs valid only over S in range per 11.4.7 rule 2; `coefref op const` → conditional quantifier); Equations → comparison terms become synthesized 0/1 indicator coefficients `IFC*` distributed over the value's top-level terms (the MAKESUNIT/E_qca shape), and one membership/element term splits the equation into complementary-domain equations `<name>A`/`<name>B` (indicator distribution would break rule 2's deemed range there); compound AND/OR/NOT, nested IF, coefref-vs-coefref comparisons, and >1 membership split per equation abort clearly. Solver: formula.c conditional-quantifier constant parse fixed — `atof` ran on the token still holding the operator, so ANY constant other than 0 silently read as 0; now parses past the operator (plus null-terminating the scalar-condvar strncpy), goldens 9/9, kit TESTE probes split VENDWMS at >1e6 / <=1e6 and recompose bit-exactly. Acceptance (test-ems_solve.R): original gtapv7.tab IF statements grafted over the shipped adaptations — VCB/VXW formulas solve bit-identically; E_qca/E_pca/E_pds equations bit-identical on common outputs (IFC1/IFC2 extra, consistent with MAKESUNIT appearing in base outputs); element probe `2 + IF[r="chn", 1]` value-verified under big3 aggregation; manual workaround section replaced with the supported spec. Remaining gaps → 6.4: IF nested inside expressions (needs solver-side evaluation), compound conditions, coefref-vs-coefref comparisons | fixes real modeler friction now; full corpus inventory + upstream-docs refresh (GEMPACK, HSL, PETSc v3.25, MPICH into `teems-docs/`) resumes after |
| 6.2 | **Condensation** (backsolve/omit variables pre-factorization) — the largest speed lever in the project: shrinks the system that costs 63% of wall, and reduces netcut as a side effect. Design settled 2026-07-15 (spec = GEMPACK manual ch. 14 + §10.16): hybrid — teems-R does the symbolic elimination (rewrite-over-surgery precedent), the solver recovers eliminated variables numerically per step from the retained defining equation (re-tagged `backsolve` statement; row evaluated after the condensed solve, before `updates_apply`; zero-pivot abort), preserving the `.bin`/`.var` contract since declarations stay. All substitutions execute as backsolves (ASB-always; the official gtapv7.sti uses 73 `b` / 0 `s`, so parity costs nothing). Interface decisions (user, 2026-07-15): `var_omit` → `omit` (hard rename); args `omit` + `backsolve` (named `c(var="E_var")` or unnamed via the `E_<var>` convention); in-TAB `OMIT/SUBSTITUTE/BACKSOLVE` honored, `SUBSTITUTE` runs as backsolve with a message; `ignore_condense` flag (ICT equivalent); no STI importer; no shipped default condensations (future model-info package); condensation status recorded in the `ems_model()` tibble (`condense`/`condense_eq` cols on backsolved Variable + nominated Equation rows); auto-omit of unshocked/unswapped exogenous vars = deploy-time follow-up (opt-in at `ems_model()`, executed in `ems_deploy()`), measure in acceptance phase before making always-on. **Phase 0 done 2026-07-16** (solver 25db887 + 38349e2): `jacobian_fill` split into build/execute + per-rank compiled-statement cache (`stmt_prog`/`sum_prog`, `jacobian_cache_free()` teardown; ownership-range guard; per-fill statement walk kept for value-dependent `zerodivide`) — collects the small-model lever found 2026-07-09 (equation parse+refill was 61% of an interactive solve): wall −52% on GTAP-RE 10.5k LU-1 Gragg (1.60→0.76s), −6% at 202k LU-1, −5% at 202k SBBD-2, RSS +3–8MB/rank; goldens 14/14 bit-identical, valgrind clean (details solver-reference §9). Backsolve rows will ride the same compiled-row machinery. **Phase 1 done 2026-07-16** (teems-R condensation engine): `R/tab_linear_parse.R` flattens linearized equation sides into signed sum-quantified coefficient×variable terms (variable-free subexpressions stay opaque, so coefficient algebra is carried verbatim) and `R/condense.R` implements the pipeline — in-TAB `Omit`/`Substitute`/`Backsolve` parsing (statements stripped before deploy; `Substitute` runs as backsolve with a message; `ignore_condense` = ICT), `omit`/`backsolve` args (`E_<var>` convention + named exceptions), the eight 14.1.10 substitution requirements validated per occurrence against the *current* rewritten equation (order-dependence per the manual) with the failed requirement quoted in the abort, rearrangement per 14.1.1 (same-pattern term combination; numeric and coefficient pivots — the latter synthesize a reciprocal `CSUB*` + zero-divide warning), substitution with index mapping + sum-index freshening, idle-sum inversion onto synthesized coefficient sums (the 14.1.12 `C00234` pattern) and ≥2-factor product hoisting for swell control (`CSUB*` coefficient+formula pairs appended at end-of-TAB — order-safe since only equations reference them); later substitutions rewrite previously retained defining equations too, so every retained equation references survivors only and phase-2 recovery `x_e = -(row·z)/a_ex` is order-free per equation. Omission is now flag-based: declaration rows stay in the model tibble (`condense`/`condense_eq` cols), write-out filters them and emits `Backsolve <var> using <eq> ;` statements; closure validation reads the flags (omit ⇒ must be exogenous, backsolve ⇒ must be endogenous), deploy-time swaps/shocks on condensed vars abort with a targeted message, and `.finalize`'s live-variable filter keeps system-size metadata honest. Acceptance so far: gtapv7.sti standard condensation (11 omits + 68 backsolves; the 4 CNT regional aggregates absent from the teems GTAPv7 variant dropped) condenses in ~11s with zero residual references and zero cross-references among retained equations (2 legitimate pivot warnings: E_ps, E_qmw); hand-verified rewrite/inversion/rule-abort unit tests in test-ems_model.R; manual model_load.qmd condensation section written. **Phase 2 done 2026-07-16** (solver backsolve statement): `tab_preprocess` recognizes `backsolve`/`omit`/`substitute` keywords (the omitted-keyword continuation was silently rewriting `Backsolve …` into a malformed `formula`; in-TAB `omit`/`substitute` reaching the solver now abort with a pointer back to `ems_model()`); `backsolve_read` marks each eliminated element in `closure_entry` (`is_backsolved` + compact recovery index; unknown variable/equation, duplicates, exogenous-in-closure abort) and an "equation"-scan filter inside `tab_next_statement(_resolved)` hides the nominated defining equations from every consumer at once (statement count, ordering, preallocation, fill, chain/partition detection) — `VecSize` drops by the backsolved element count and `backsolve_validate_refs` rejects any retained-equation reference to a backsolved variable. Per-step recovery (`backsolve_recover`) rides the phase-0 compiled statement programs (build half extracted into `stmt_prog_build_one`, shared with `jacobian_fill`; `force_all` skips ownership pruning): after each solve, before `updates_apply`, each defining equation's rows are evaluated with the step's known changes (endogenous from the solution vector, exogenous from `exo_z` captured at every vece fill site) and `x_bs = −(row·z)/pivot` feeds a third branch in the update loops (Johansen, both Gragg step loops, terminal smoothing — varchange/xcf/substep_base/clag1 mirror the endogenous branch, so extrapolation, accuracy reporting and the `.bin`/`.var` contract cover backsolved elements automatically); same-pattern pivot occurrences combine into the algebraic pivot (the E_ps `1−CSUB43` coefficient-pivot case), zero pivot and row→element bijection failure abort the run. Gates: goldens 14/14 bit-identical with the feature unused (re-run after the preprocess keyword change), warnings 102 = baseline, valgrind on a condensed Gragg run 0 leaks with only the 6 pre-existing string-overlap contexts, 5 negative-path aborts verified by hand. Acceptance (.audit/deploy_bs_accept.R + accept_backsolve.R): GTAPv7 standard condensation (11 omits + 68 backsolves; system 3,485→989) and GTAP-RE (7 backsolves) A/B vs uncondensed across Johansen/Gragg × LU/DBBD-2/SBBD-2 × nsubints 1/2 — every combination solves; core backsolved values match the uncondensed solve to ≤5e-6 absolute (float32 coefficient storage noise; GTAP-RE ≈1e-8), bulk median relative difference 1.8e-7, with larger deviations confined to the welfare/CNT summary variables whose plain-model Johansen-vs-Gragg cross-method noise is the same order (EV 0.14, CNT* 2.4%); exogenous-shock identity `pfactwld == shock` exact at ns1, and the 1.4e-8 ns2 deviation is byte-identical in the uncondensed run (pre-existing subinterval rounding). teems-R `.dock_tail` fixed (compose broke on condensation-freshened declaration indices like `MARGm1`). teems:dev/latest rebuilt with the backsolve solver (6c66da989b0b; old = teems:pre-backsolve-backup) and the teems-R suite is green against it, 849/849 including a new condensation e2e in test-ems_solve.R (condensed GTAPv7 with omit + backsolve incl. the ps coefficient-pivot case: recovered values match the uncondensed run at 1e-4, omitted variables absent, pfactwld identity intact). **Bench A/Bs done 2026-07-17** (.audit/ab_condense.sh, interleaved pairs, medians of 3, same binary both sides): bench-static 1.35M (GTAPv7 R32×medium, full standard condensation, system 1,354,880→55,604 = −96%) Gragg LU-1: 265.5s→157.0s = **−41% wall**, RSS +13% (the condensed matrix is ~9× denser per row and carries 551 CSUB arrays); bench-inter 202k (GTAP-RE WB7×macro T0–20, 7 backsolves = −7% system) Gragg SBBD-2: 4.65s→5.33s = +15%; bench-inter-L 4.36M (WB7×medium, same 7 backsolves = −17% system) Gragg SBBD-2: 149.9s→339.4s = **+126%**, RSS +61%. Follow-up A/B with the FULL standard condensation ported to GTAP-RE (all 68 backsolves port rule-clean, 9 of 11 omits exogenous in the RE closure) settles the intertemporal question: bench-inter 202k −77% system: 4.65s→7.9s (+69%); bench-inter-L 4.36M −94% system (4,362,953→256,949): 155.1s→765.0s = **+393%**, RSS +63%. REVISED RULE: condensation is an LU-era lever — it pays on static models solved by plain LU (−41% at 1.35M) and is counterproductive under the bordered methods at every elimination share tried, because substitution destroys the intra-block sparsity SBBD/DBBD exploit (per-block MA48 cost explodes with density) while per-step backsolve recovery re-evaluates the eliminated rows anyway. GEMPACK needs condensation because dense-matrix size is its binding constraint; TEEMS's bordered methods already collect that win from structure. A cross-method check (user request) closes the loop: plain SBBD-2 vs fully-condensed LU-1 on GTAP-RE — 202k: 4.65s vs 10.4s (condensed-LU ≈ plain LU-1's historical 10.3s, i.e. condensation bought LU nothing); 4.36M: 155s vs 27m36s (10.7×; also worse than condensed SBBD-2's 765s — the dense 257k core hurts one global LU factorization even more than per-block factorization). Structure exploitation beats size reduction at every point measured on the intertemporal model; the 4.36M condensed-LU run also exercised the nexo>VecSize widened-B path in production (703k exogenous vs 257k unknowns). Static DBBD cross-check (user request; single probes, ±8% pair noise): plain DBBD-2 on bench-static 437.6s, DBBD-2 with the maximal DBBD-compatible condensation (full std minus qxs/pcif/pfob/qtmfsd/pfd so nexo 257,164 ≤ VecSize 282,644; −79% system) 472.2s (+8%, RSS +43%) — the full standard condensation is refused by the nexo>VecSize guard. Both DBBD configs are ~3× slower than fully-condensed LU-1 (157.0s), and plain LU-1 (265.5s) also beats plain DBBD-2 on this rig: for this static model LU is the right method and condensation compounds it, while condensation again buys the bordered method nothing. Ship guidance: condense for LU static runs (or memory-bound cases); do not condense bordered intertemporal runs, and do not use condensation to make LU viable where a bordered method applies — a teems-R advisory and probe-informed advice via 6.10 are follow-ups. PLANNED (2026-07-17): repeat the full benchmark comparison (methods × condensation matrix, all bench rigs) on HPC with 32 CPUs / 128 GB RAM — the local A/Bs ran the bordered methods at only 2 ranks, so their parallelism (35 region blocks static, 21 time blocks intertemporal) was mostly unexploited; the LU-vs-bordered and condensation rankings may shift materially at 16–32 ranks and larger memory headroom (XXL 8.3M rig becomes feasible). NB omission (unlike substitution) causes no densification — the deploy-time auto-omit of unshocked exogenous variables retains its separate rationale. The bench runs also surfaced and fixed a real defect (746cb01): heavy condensation leaves nexo > VecSize, overflowing the square-B shock vector (glibc heap corruption at 1.3M backsolved elements) — vece/B columns now span max(VecSize,nexo) under the sequential methods (goldens bit-identical), while DBBD/NDBBD abort cleanly pending a layout-aware B preallocation; and default -laA 300 is too small for 1.35M-unknown static LU (needs ~450; the dense condensed system needed 2000) — auto-laA is a candidate rider for the 6.10 probe. teems:dev/latest rebuilt (27c643a722d9), condensation e2e green against it | highest-leverage item overall |
| 6.3 | **Stepping methods** (in cost-benefit order): (a) Euler multi-step ✅ **done 2026-07-18**: `-solmed Euler` = the Gragg driver with forward steps throughout (`updates_apply` flag 0, no leapfrog, no terminal smoothing solve) and h-series Richardson weights (step ratios unsquared, per the caveat below; weight formulas hoisted to one site, bit-neutral for Gragg); steps must be strictly increasing (new shared check), parity rule now Gragg-only. Goldens 14/14 bit-identical ×2 with the feature unused; valgrind clean after plugging the skipped-smoothing-path vece leak. Acceptance (GTAP-RE rigs): null-shock solution exactly zero; pfactwld shock identity exact; on a float64-coefficient diagnostic build under a 40 % shock the extrapolated error contracts ~8× per step-count doubling (the h³ rate; Euler 8-16-32 ≈ Gragg 2-4-8 accuracy), while on the production float32 build Euler-vs-Gragg deviations sit at the measured method-independent per-pass noise floor (Gragg 32-64-128 self-diff is the same order; the h-weights amplify it |w|₁=5 vs Gragg ≈1.5, observed 3.7×). teems-R `solution_method="Euler"` + increasing-steps validator + docs/snapshots ride along. SIDE DISCOVERY fixed en route (solve_drivers.c): since 746cb01 every multi-rank DBBD/NDBBD run spammed PETSc "Can only get local values, trying -1" — the explicit vecb creation dropped the `VEC_IGNORE_NEGATIVE_INDICES` option that `VecDuplicate(vece)` used to inherit, and the (N)DBBD back-solves probe vecb with -1 sentinels; outputs were never affected (sentinel batches contribute zeros by design — post-fix goldens bit-identical, gate logs 0 errors) but teems-R's log grep aborted static DBBD e2e runs; (b) Gragg terminal smoothing ✅ **verified already present** (2026-07-13, code inspection, no change needed): `Mmid` is the full Gragg method, not plain modified midpoint — per solution pass: Euler start (solve_drivers.c:1110), midpoint leapfrog z_{m+1}=z_{m−1}+2h·f(z_m) (`clag1+2*x1`, :1150; coefficient side via `updates_apply(midpoint=1)`), then a post-loop Jacobian rebuild+solve at the final state (:1289) feeding the smoothing ½(z_n+z_{n−1}+h·f(z_n)) (:1604 change / :1616 percent) — the (n+1)-th evaluation that manual §30.2 says distinguishes gragg from midpoint; corroborated by the 1/(r²−1) Richardson weights (:1699), valid only for the even-power (h²) error expansion the smoothing guarantees. No unsmoothed-midpoint option exists — if plain-midpoint parity is ever wanted it's a subtraction, not an addition. Renamed accordingly 2026-07-13 (`Mmid`→`Gragg`): canonical `-solmed Gragg` + `SM_GRAGG`/`solve_gragg` (solver b217d58; `Mmid` warns as deprecated alias, stats.json normalized; unknown `-solmed` now aborts instead of running uninitialized, 1ec3e98), teems-R `solution_method="Gragg"` + ems_solve docs with method summary and Pearson reference (ff57928), manual solve.qmd (30fe55e); goldens 14/14 bit-identical, local teems:dev/latest image rebuilt (old kept as `teems:pre-gragg-backup`) — published-image refresh rides the 6.6(d) batch. Literature anchor (2026-07-13, `teems-docs/pearson_solver.pdf` = Pearson, K.R. (1991), *Solving Nonlinear Economic Models Accurately via a Linear Representation*, Impact Project Preliminary Working Paper IP-55, rev. June 2002): eq. 6.1 / Algorithm 7.1.2 (change form) and §7.2.4 (percentage-change form) are the implemented spec verbatim, including the 2-/3-solution extrapolation formulas (§6, the [4w(2N)−w(N)]/3 and 1/N⁶ combinations = the code's 1/(r²−1) weights); Pearson's footnote 11 attributes the method to Gragg (1965) A(t;h) — his "modified midpoint" IS Gragg's method, and his "explicit (or unmodified) midpoint" (weakly stable, parasitic modes; his robustness point 5) is what GEMPACK manual §30.2 calls "midpoint". CAVEAT: Theorem 6.1 (Gragg) states the even-power h² expansion **for even N only** — enforced 2026-07-13: teems-R's validator now aborts on any odd step count under Gragg (teems-R 5475781; the old mixed-parity check also misnamed the argument as n_subintervals in its message). NB for (a): Euler's error series is in h, not h², so the h² extrapolation weights do NOT carry over to Euler multi-step; (c) Runge-Kutta ✅ **done 2026-07-18** (solver 21eddf9, spec = Schiffmann 2022 / GM §26.5–26.5.2): `-solmed RK2/RK4/BoSha32/DoPri54` via a Butcher-tableau driver (`solve_rk.c`) — one Jacobian refill + linear solve per stage, so LU/SBBD/DBBD/NDBBD all work unchanged; stage states are realized by per-step snapshot/restore plus **product-form updates** (KEY LESSON: `updates_apply(0)` linearizes multi-factor product updates — an O(h²) coefficient error per stage that caps every flavor at global first order; measured before the fix as ~2×/doubling with RK4≈DoPri54, textbook orders after), with stage combination in the cumulative-solution scale (paper eq. 7: a stage percent V contributes (1+X/100)·V). Embedded flavors propagate component-wise error estimates (eq. 6/8) into a doubles `.acc` beside `sol.bin` plus a logged face value (0.02 brackets), and support `-adaptive yes|accuracy-only` with the eq. 11 controller (0.85 safety factor, q∈[0.5,2], accept/reject MPI-collective via Bcast from rank 0; `-epstol`/`-retryadj`/`-maxretries`). Single `steps` count; subintervals and extrapolation excluded by design (abort). Gates: goldens 14/14 bit-identical feature-off ×2; valgrind 0 lost (contexts = pre-existing string-overlap class through the new call sites); null-shock exact zeros, pfactwld identity exact (1.0, cleaner than Gragg's ns-rounding); f64-diagnostic ladder at 40% shock shows h² (RK2 ~4×/doubling), h³ (BoSha32 ~8×), h⁴ (RK4 ~16× down to the Gragg-truth floor ~5e-7), DoPri54 floor-limited from 2 steps; embedded estimates bound the worst true metric (BoSha32 at all n; DoPri54 apparent violations confined to cancellation-amplified welfare/CNT aggregates at the truth floor — the paper's documented case two); cross-method RK4 max 8e-9 (SBBD-2) / 3e-8 (DBBD-2) / 4e-7 (NDBBD-2) vs LU; production float32 deviations vs Gragg only in the documented EV-class noise (medians ~1e-8). Value arrays stay resident (no `!inmemory` spills — the stage restores need the step snapshot addressable). Adoption gate (fixed-accuracy wall vs Gragg 2-4-8 on the bench rigs) deferred to the HPC benchmark matrix with the other method rankings; (d) Adams-Bashforth — ONE factorization per step at order k (vs RK4's 4): directly attacks the factorization wall; RK startup; stability caveats; (e) implicit stiff methods (Radau/BDF/ode15s) — parked: performance-negative for smooth CGE paths; revisit only if adaptive RK step-size collapse demonstrates stiffness; (f) **LARGE-SHOCK PROGRAM (user question 2026-07-19: how to take very large sustained shocks on intertemporal models without losing accuracy — recommendation recorded, none implemented)**: ranked plan — (f1) *error-driven nonuniform subintervals*: today `nsubints` splits the shock uniformly and blind, but large shocks stress the path nonuniformly (steepest where the shock bites and near levels asymptotes/−100% guards); apply the existing RK adaptive machinery (embedded per-component metrics with cumulative propagation = the `.acc` plumbing, MPI-collective accept/reject, retry flags) to the SHOCK dimension — solve a subinterval, accept-and-stride or reject-and-bisect, exact data re-anchor at each accepted boundary; composes with every solmed; highest value per effort. (f2) *full Bulirsch-Stoer*: Gragg IS the GBS kernel — extend the fixed 2-4-8 ladder to adaptive extrapolation depth + adaptive step from the tableau's own error estimates; `-fastrefac` changed its economics (deeper ladders = more same-pattern factorizations at JOB=2 prices). (f3) *opt-in f64 coefficient pipeline* (`store_real=double` production option, ~2× coefficient-array memory; diagnostic build recipe exists): MEASURED enabler, not a method — production f32 ladders anti-converge (per-pass update noise random-walks, extrapolation weights amplify; floor ~2.7e-6) while f64 builds converge to ~5e-7+; every high-accuracy method hits the f32 ceiling exactly when a large shock makes you throw steps at it; method + precision is the package. (f4) implicit/arc-length continuation with true Newton correctors = the research-grade answer but BLOCKED on levels residuals (GEMPACK-class systems are linearized-only, no F(x)=0 evaluator); the implementable fallback is iterated re-linearization (fixed-point implicit midpoint, 2–3 JOB=2 factorizations per step) which cannot correct linearization drift, only stabilize; unlocks if TAB levels-equation support ever lands. (f5) *demotes (d) Adams-Bashforth*: its value was fewer factorizations — an economy `-fastrefac` already collected — and explicit multistep stability regions are the wrong direction for stiff large-shock paths | RK gate: fixed-accuracy wall-time vs Gragg 2-4-8 |
| 6.4 | Statement completion per 6.1 ranking: full IF semantics (opcode skeleton exists: `OP_IF_*`), POSTSIM, others | each zero-cost when unused |
| 6.5 | **Netcut enforcement** (border size = interface-problem size, governs bordered-method viability): E1 validate in `ems_model()` ✅ (2026-07-10, teems-R: `.check_netcut()` in `R/chk_netcut.R`, wired into `.process_tablo` after maths parse — mirrors the solver's border rule (jacobian.c `var_inter`: any lead/lag reference borders the variable's entire element block) by scanning Equation definitions for variable references carrying an index offset and warning (`model_wrn$netcut_inflation`) when such a variable is declared over ≥2 non-`(intertemporal)` sets — the minimal-proxy shape (one non-time dim, e.g. `kb(REG,TIME)`) passes silently and all shipped models are warning-free; set sizes are unknown at `ems_model()` time so dimension count is the structural signal; warning carries the proxy recipe, manual §netcut added, snapshot + no-warning tests in test-ems_model.R); E2 auto-rewrite minimal intertemporal proxies during TAB assembly ✅ (2026-07-10, teems-R: `.rewrite_tab_netcut()` in `R/tab_netcut_rewrite.R`, runs right after the IF rewrite in `.process_tablo` — lead/lag variable references that fix ≥1 dimension to a quoted element (the only statically-guaranteed reduction; corpus scan found *zero* violating models — INT_SCET/GTAP-RE/GTAP-INT all link periods via minimal REG×TIME variables already, so E2 guards new models) get a synthesized proxy variable `NCV*` over the remaining dims + linking equation `E_NCV*` inserted after the source declaration, the lead/lag moved onto the proxy, `orig_level` qualifiers dropped, other qualifiers inherited, proxies cached per (variable, element-slice), always-on with a cli inform listing rewrites; `.check_netcut()` skips synthesized proxies; full-set references remain E1-warned. Verified: rewritten slice-lead GTAP-RE graft solves bit-identically to a hand-proxied reference (SBBD-2, common outputs identical, proxy values == hand-proxy values; test-ems_solve.R) and netcut drops 207→36 on the probe (stats.json A/B with rewrite disabled via assignInNamespace). Solver untouched — quoted-element refs in equations were already supported, e.g. GTAP-INT `qo("capital",r,t)`); E3 solver-side element-level border classification ✅ slice level (2026-07-17, `border_mark_ref` in jacobian.c + per-element `ele_inter` marks consulted by `block_var_count` and the three numbering passes): a lead/lag reference now borders only the element slice it spans — fixed dims (tab_preprocess singleton subsets, so quoted elements and any proper-subset index narrow the mark via `superset_pos`) × full chain extent — collapsing to the old whole-variable flag when the slice covers the variable, so every model without sliced lead/lag references classifies identically (goldens 14/14 + accept_structural 4/4 bit-identical; warnings baseline; valgrind 0 lost, rank-0 contexts unchanged, HSL worker rank +8 contexts in the pre-existing uninitialized-size class it shares with var_inter/closure_vals). Acceptance graft (GTAP-RE + `ncve3(r,t)=qpd("crops",r,t+1)` sliced lead, real-shock rig): netcut 72→36 vs pre-E3 binary — exactly the 36 non-crops qpd elements — SBBD-2 and NDBBD-2 solve, solutions match baseline within rounding (max 2.6e-8, median 2.4e-14), pfactwld shock identity and the defining-equation identity exact; detection/probe scoring is element-aware automatically (probe allocates its own marks). The equation-scan filter means classification sees the condensed system, so backsolved equations never contribute marks. EXACT CHAIN MARKS + MISMATCH TRIGGER ✅ (2026-07-17, same session): each dim of a non-block-safe reference now marks exactly its index set's positions shifted by that dim's offset and clipped (chain dim included), and the trigger extends beyond lead/lag offsets to any chain-dim index other than the equation's chain quantifier — closing the latent cross-block hole where a chain-quantified equation references the chain set through a sum index or fixed element (time-side analog of the reg dimname rule; proven real on a `ncvsum(r,t)=0.25*sum{tt,ALLTIME,qpd("crops",r,tt)}` graft: the pre-E3 binary got ncvsum wrong by 3.1e-4 under DBBD/NDBBD while stage 2 is exact to 1.3e-10; SBBD was never correctness-exposed — HSL_MP48 derives per-block column structure from the actual sparsity, so misclassification there costs performance only). Boundary-period elements of ordinary lead variables leave the border: GTAP-RE netcut 27→18 (−33%), sliced-lead graft 36→24, with IDENTICAL solver-reported accuracy distributions. GOLDEN RE-ANCHOR (the shift was confined to exactly nsub-sbbd + nsub-ndbbd — the only real-shock bordered gates; static, LU (no chain resolved under -matsol 0), and null-shock bordered gates stayed bit-identical): baseline binary reproduced the old manifests bit-identically for clean attribution, old-vs-new solutions equivalent (sbbd max 1.5e-9; ndbbd max 9.8e-7 on level-scaled $-million change/summary vars ≈ 1e-9 percent-change noise × world-scale levels; medians ≲1e-12; pfactwld identity at known ns-rounding), pre-E3 manifests kept in `.audit/pre-e3-manifests/`, both manifests re-anchored, full suite re-verified | **E1+E2+E3 done** (element-exact, 2026-07-17) — value-aware classification (banded adjacency ≈ partitionable, pairs with 6.8 detection) remains the open follow-up |
| 6.6 | **Image fix batch** (absorbs 6.9, 2026-07-09): the current published approach is broken, not merely slow — amd64 base image built `-march=native` (SIGILL on CPUs older than the build host: the published binary carries 42,731 AVX/AVX-512 instructions from static `-march=native` PETSc) while the solver's own objects carry no `-march` at all. One coordinated change: (a) `hsl` → `teems-solver` binary rename ✅ (2026-07-10: makefile target, expedited/full Dockerfiles copy the new name and add a one-release `hsl` transition symlink in the runtime image, docs/README/verify.sh paths updated, teems-R invokes the new path (ee76e2a; cmd-string snapshots re-anchored); rename verified output-neutral — all 9 goldens bit-identical with the renamed binary; `teems:dev` built with rename + flag plumbing, GTAP-RE SBBD-2 solve OK; local `teems:latest` re-pointed at it, old image kept as `teems:pre-march-backup`. NB the pre-existing GTAP12 dynamic cross-method failure now manifests as numeric disagreement instead of a bound abort — new flags shift numerics on that borderline data; static sibling now passes; still owned by feature/GTAP12a); (b) psABI-level image tags ✅ **plumbing done** (2026-07-10): `MARCH` build arg in base/full Dockerfiles (amd64 default `x86-64-v2`, arm64 `armv8-a`, never `native`); resolved flags recorded in `${BUILD_DIR}/archflags` and consumed by the expedited layer (`make OPT="-Ofast $ARCH_FLAGS"`, HSL configures `-O2 $ARCH_FLAGS` — deliberately not `-Ofast` to keep MA48 pivoting semantics), so base/solver levels cannot mismatch; expedited gained `BASE_IMAGE` build arg for variant selection; MPICH now `--enable-fast=all,O3` + `MPICHLIB_*FLAGS` (arch flags kept out of the mpicc wrappers per install guide), PETSc `--with-memalign=64 --with-cxx=0 --with-fortran-bindings=0` (docs reviewed from teems-docs/mpich + teems-docs/petsc clones). Validated end-to-end: base v2 build clean, expedited picks up flags, binary has 0 ymm/zmm instructions (vs 42,731 in current image), GTAP-RE SBBD-2 deploy+solve OK via `docker_tag="v2-test"`. NB MA48's dgemm/dtrsm resolve to Debian OpenBLAS (runtime CPU dispatch) — BLAS3 already optimal per host; `OPENBLAS_NUM_THREADS=1` A/B queued for the batch. Maintainer guide at `docs/image-publishing.md` (variant matrix, buildx/publish/manifest commands, validation checklist, benchmark gates); (c) teems-R selects the tag from host capability ✅ (2026-07-14, teems-R 2c9d773): `R/docker_tag.R` — `.supported_isa_levels()` probes the host (Linux `ld.so --help` with `/lib64/ld-linux-x86-64.so.2` fallback; macOS `sysctl` leaf7 AVX2; arm64 → `armv8-a`; unknown x86-64 → v2) and `.resolve_docker_tag()` prefers the highest supported level with a local `teems:<level>`/`teems:vN` image, falling back to `latest`; explicit `docker_tag` always wins; inform on auto-pick; unit tests + snapshot in test-ems_options.R; manual options.qmd updated; (d) golden refresh + image rebuild ✅ (2026-07-14): audit container rebuilt on the v2 base (HSL libs at `-O2 -march=x86-64-v2`, shared libs installed to `/usr/local/lib`; `verify.sh` build now mirrors the expedited `make OPT="-Ofast $ARCH_FLAGS"` — plain `make` had left solver objects arch-less), golden commands stripped of the removed/inert flags (`-regset`/`-enable_time`/`-nesteddbbd`/`-presol`), and all 14 manifests re-anchored (old ones in `.audit/premarch-manifests/`, pre-batch reference outputs in `.audit/premarch-ref/`). KEY FINDING: the x86-64-v2 scheme is bit-identical to the old build across the whole suite — v2 adds no FP-semantic instructions (FMA arrives at v3), so the re-anchor delta is exactly the 6.8 flag-removal delta, which was validated separately (see 6.8). `teems_base:v3` + `teems:abv3` built for the A/B; new v2 image = `teems:dev`/`latest` (old kept as `teems:pre-flagfree-backup`), binary carries 0 `%ymm`/`%zmm` instructions (v3 sibling: 45k — NB the historical "42,731" count was inflated by greping `ymm` inside `Symmetric` symbol names; the register-syntax census is the honest one). teems-R full suite 720/720 vs the new image. A/Bs ✅ (2026-07-14, `.audit/ab_march.sh` through the runtime images, idle box, interleaved pairs): **v3 no win** (SBBD-2 4.36M eq medians v2 125.5s / v3 128.0s; static LU-1 neutral) → publish v2 only, `teems:abv3` stays local evidence; **`OPENBLAS_NUM_THREADS=1` ~20% win** (103.5s vs 128.7s, decisive every pair) → baked as `ENV` into expedited+full runtime stages (probed golden-bit-identical, no re-anchor; final image dbe303439c46 = teems:dev/latest, e2e 41/41, baked-ENV bench run 102.2s). Results in solver-reference §9 + image-publishing §6. REMAINING: the actual publish — buildx push of `teems_base` x86-64-v2 + armv8-a (arm needs a native builder or QEMU) + `latest` manifest, user decision 2026-07-14: publish only when all work is done — follow the pedantic release runbook at `docs/image-publishing.md` §4 (phases A–F: preconditions incl. version-label bump, amd64+arm64 pushes, latest manifest, post-push validation incl. audit-rebuild-from-published-base, release notes). **(e) VERSION HANDSHAKE (adopted 2026-07-19, user decision; supersedes the hsl transition-symlink schedule — the symlink is dropped AT v2.0.0, not one release after)**: solver and teems-R jump v1→**v2.0.0** together and compatibility becomes an explicit contract instead of name continuity. Solver: one version constant surfaced three ways — `-version` flag (print `teems-solver 2.0.0`, exit 0; a pre-v2 image fails this query, so no-response IS the v1 detection), an ungated startup banner line (every archived solver_out records its producer), and a `solver_version` field in stats.json (composed results carry provenance); Dockerfile `org.opencontainers.image.version` labels (currently hard-coded "1.0.0") set from the same constant so `docker image inspect` is a zero-cost pre-pull hint (label = hint, `-version` = ground truth). teems-R: pre-flight in `ems_solve()` AND `solve_in_situ()` (the in-situ path bypasses docker), cached per tag/binary per session — ABORT on major mismatch or no-version-response ("image predates the versioned interface — update to ≥2.0.0", naming the pull command), WARN on minor/patch skew; escape hatch `ems_option` (`version_check = "abort"/"warn"/"off"`) because bit-exact reproduction against pinned old images is a stated project value. Policy after the lockstep jump: the MAJOR is the interface-contract version (solver 2.x + teems-R 2.y interoperate; a teems-R release that needs a new solver feature declares a minimum solver floor) — do not re-manufacture skew as spurious exact-match warnings. RATIONALE (from measured failure classes): new-flags-on-old-solver is silently ignored by the options parser (wrong-configuration solves, cf. the RK-e2e-vs-old-image incident), old-callers-on-new-image was only half-covered by the symlink, and image staleness (`teems:dev` pre-fastrefac) is currently tracked by hand; a version fence is also the safe mechanism for the planned `-fastrefac`/`CNTL(2)` default flips (teems-R knows which default regime the binary has). Release note simply states: v2 teems-R requires v2 images | needed fix; coordinate teems-R + images; one retagging for everything |
| 6.7 | **HSL catalogue exploration** — assess additional HSL routines for speed/robustness across all solution × matrix methods; ranked shortlist with rationale in `teems-docs/hsl-catalogue-candidates.md` (top: MC64 pre-scaling, MA41/MA38 multifrontal alternative, MC33 bordered-block-triangular ordering vs netcut, HSL_MC79 Dulmage-Mendelsohn closure diagnosis). Retrieve specs/code for survivors; golden + A/B benchmark gates apply. **Base-set study done 2026-07-18** (full specs of ma48/ma51/hsl_mc66/hsl_mp48/hsl_ma48 read; instrumented phase timings): the dominant defect was structural, not a knob — every step re-ran the MA48 analyse (82% of MA48 time at 1.35M: 12.2s analyse vs 2.6s factorize per step, MA48 = 94% of wall) and both fast-refactorize mechanisms (MA48BD `JOB=2`, MP48 `FACT_JOB`) were unused; the SBBD path also rebuilds its whole MP48 instance per step. Knob verdicts: `CNTL(2)=0.01` (HSL's newer default) −12.5% factorize / −4% wall, solution shift ≤3e-8 ≪ noise floor (adoption-ready, compose with refactorize); `ICNTL(4)=0` full Markowitz pathological at scale (65 min in first analyse — reject); `ICNTL(5)=64` immaterial; MC66 knobs unreachable in production (opt-in `-withmc66` only). **Persistent refactorize phase 1 landed 2026-07-18**: `-fastrefac` (default off) — sequential-LU pivot sequence persists across steps/stages (Gragg loop, terminal smoothing, all RK stages), repeat factorizations run `MA48BD JOB=2` with `ICNTL(11)=1` + decline fallback, extraction keeps explicit zeros (pattern must not be value-dependent), and `laA` self-sizes by retrying with MA48's suggested size on `-3` (auto-laA). Gates: goldens 14/14 bit-identical flag-off; flag-on golden-re bit-identical, RK4 bit-identical to flag-off, real-shock max_rel 1.1e-8, valgrind 0 lost/6 baseline contexts; recovered a previously fatal `-laA 300` run. A/B bench-static 1.35M LU-1 Gragg: **−72% wall** (4:25→1:15), +21% RSS. **Phase 2a (SBBD) landed 2026-07-18**: persistent MP48 instance under the same `-fastrefac` flag — border lists, per-block pivot sequences and factors live across steps (module-saved `MP48_DATA`, `ICNTL(13)=1` user-owned solution vector, JOB=6-survivor arrays released on rebuild); repeat steps refill `VALUES`/`B` and run JOB=4 `FACT_JOB=2` + JOB=5, declined fast path rebuilds the instance; redo decision broadcast from the host so all ranks branch together. Gates: goldens 14/14 flag-off; flag-on golden-re SBBD bit-identical; real-shock max_rel 3.0e-8; valgrind 0 lost and *fewer* rank-1 contexts than flag-off (116 vs 127, the skipped per-step phases carried part of the known class). A/B: **−36% wall at 4.36M** (87.1→56.2s, RSS +28%), −25% at 202k. **Phase 2b (DBBD) + bordered-methods audit pass 1 landed 2026-07-19**: per-diagonal-block persistent factors (`SPEC48M_MSOL_P`, `dfr_*` statics double as the within-step back-solve handoff — no scratch files under the flag; flag read inside `dbbd_solve` so every driver inherits it; declined `JOB=2` re-stages from the still-live submatrix). Correctness: goldens 14/14 flag-off; flag-on golden-re DBBD bit-identical AND nsub-dbbd real shock *exactly* identical to flag-off; valgrind identical profiles both ways. Performance verdict is honest: −4% at 202k, −1% (noise) at 1.35M static — analyse is NOT the DBBD bottleneck (border-column solves + per-step submatrix extraction dominate); the DBBD leg's value is method-consistency + eliminating per-step factor scratch-file I/O in disk mode. Audit findings: **fixed** (a) `dbbd_solve` `obiviindx1/0` realloc-on-indeterminate-pointer UB every step (the NDBBD copy had the fix; divergent-copy defect — likely the historical pathological-partition abort class), (b) first-generation `Bindices` IS leaked per block per step (54–67 KB/step/rank → ~2 KB residual). **Audit pass 2 landed 2026-07-19**: (a) empty-border-range guard in the `[Istart,Iend)` row-slice scans (dbbd_solve + ndbbd twin — a rank owning no border rows fed stale `j/j1` to `VecGetValues`; unreachable at current geometries, real at HPC rank counts); (b) DBBD residual leaks eliminated — the five hand-`PetscMalloc`'d submatrix arrays were orphaned when `MatCreateSubMatrices(MAT_INITIAL_MATRIX)` allocated its own arrays over them (the NDBBD twin never pre-allocated, hence its clean valgrind); DBBD valgrind now 0 bytes lost both flag states; (c) DBBD extraction persistence under `-fastrefac`: the five submatrix arrays persist and refill via `MAT_REUSE_MATRIX` (ordering-snapshot guard, mismatch → fresh build) — DBBD 202k −4%→**−13%**, static at parity. KEY LESSON: `MatTranspose(MAT_REUSE_MATRIX)` per step was a +6% regression on 1.35M static (it re-walks the pattern each call); transposes stay per-step `MAT_INITIAL`. Still noted for follow-up: `nmatinplus` re-broadcast fragility, `ndbbd_fac_emit` slack writes, OMP-atomic accumulation contention (measure at HPC thread counts), per-column border solves, NDBBD extraction persistence (same `MAT_REUSE` pattern would port to `submatAij/Bij/Cij`; presolve-file traffic is its large-rig bottleneck). **NDBBD regional-block persistence landed 2026-07-19** (`PREP48M_MSOL_P`; `ndbbd_fac` store extended with the MA48 JCN mapping + staged nz as per-slot readiness; per-TIME interface blocks keep the per-step flow; flag forces the resident store in NDBBD's default disk mode): goldens 14/14 flag-off; flag-on golden-re NDBBD bit-identical, nsub-ndbbd real shock *exactly* identical; valgrind 0 lost, fewer contexts than flag-off. A/B: −9% at 202k; noise-dominated at 4.36M (flag-off spread 276–318s exceeds the effect — NDBBD's large-rig wall is presolve-file I/O + merges, not analyse). With this, `-fastrefac` covers all four matrix methods. **Adoption plan (recorded 2026-07-19)**: at the next golden re-anchor slot, and after the HPC benchmark matrix confirms behavior at 16–32 ranks, (1) flip the `-fastrefac` solver default to ON for LU/SBBD/DBBD; NDBBD's interaction with its default disk-backed factor store stays opt-in (the flag forces residency — the one genuinely risky memory interaction), i.e. default-on only where residency was already the norm; keep `-fastrefac 0` as the escape hatch for memory-ceiling runs and bit-exact reproduction of pre-flag archives; NO teems-R argument — power users reach the flag via `append_args`, and the RSS trade-off (+21–28% measured) gets a line in the solve docs. (2) In the same re-anchor, adopt `CNTL(2)=0.01` as the F77 MA48 pivot threshold at all factorize sites (HSL's own newer default; measured −12.5% factorize / −4.1% wall on static LU, real-shock solution shift ≤2.9e-8 ≪ noise floor, composes with `-fastrefac`), with a solver-level `-ma48u <x>` override (old behavior = `-ma48u 0.1`), again with no teems-R argument. Both changes shift outputs within rounding ⇒ all manifests re-anchor together; validate per the flag-removal playbook (shock identity, cross-method, ns-convergence, keep pre-change manifests). HSL_MA48 v3 migration assessed and deferred: same MA50 kernels (no speed over `JOB=2`), auto-workspace/long-int benefits, but opaque derived types break the C↔Fortran resident-factor handoff and force golden re-anchors — revisit at a re-anchor slot or for XXL-scale long-integer need. **MC64 pre-scaling implemented, evaluated and DROPPED 2026-07-19** (shortlist item 1; was `-mc64`, default off, riding `-fastrefac 1`; the implementation was reverted after evaluation — this entry is the rejection record): `mc64d.f` compiled into the solver (its only external, `MC21AD`, already ships in mp48's `ddeps.f` — never stage mc64's own `ddeps.f`, duplicate MC21/MC59 symbols); `SPEC64_SCALE` wrapper builds CSC from the staged COO and runs `MC64A/AD JOB=5` (max-product matching), returning `exp(u)/exp(v)` row/col factors — permutation output deliberately unused (MA48 keeps its own pivoting; the spec's factors apply to the *unpermuted* matrix). Factors compute once per solve at the analyse state and ride the `-fastrefac` persistent staging: LU refills apply per-entry products and recompute on any fresh analyse (declines, workspace growth); SBBD scales the whole assembled system at MP48 instance (re)build (pattern unchanged ⇒ partition/border lists unaffected); rhs scales by `Dr`, solutions unscale `x = Dc·y`; MC64 warnings (structural singularity, overflow-risk factors) fall back to the unscaled solve with a note. DBBD/NDBBD per-block scaling deferred. Gates: goldens 14/14 bit-identical flag-off; flag-on golden-re LU null shock **bit-identical** (zero rhs scales to zero); nsub-lu real shock max_rel 2.1e-8 / median 5.6e-16, nsub-sbbd 2.1e-8 / 4.4e-16 (floor 2.7e-6); RK4 flag-on null shock exact zeros; valgrind 0 lost / 6 baseline contexts. A/B verdict (well-scaled bench corpus, `-fastrefac 1` both sides, interleaved): static 1.35M LU-1 −4.4% wall (72.4→69.2s) / +9% RSS — but the win does NOT scale: 1.62M sparse static (163reg×5comm XL) +3%, **8.3M dense static (163reg×23comm XXL) +11% wall** (19:04→21:10 medians) / +10% RSS, 202k SBBD-2 +5%, 4.36M SBBD-2 +2.5% — the JOB=5 matching cost grows faster than any factorize benefit (the 1.35M gain was the outlier, not the trend); `-laA 300` probe still auto-grew to the equivalent of 600 (no fill/workspace relief); JOB=2 decline rates already zero. The robustness-insurance claim was then TESTED EMPIRICALLY (2026-07-19, badly-scaled-model sweep: golden-nsub-re data uniformly rescaled ×1e6/×1e9/×1e12 — units change preserves every accounting identity and leaves the percent-change solution mathematically invariant, so drift vs the native-units run measures pure numerical damage; change-class elements excluded via the `.var` `change_real` flag): ×1e6 ($ instead of $-million) percent-class max drift 7.6e-5 vs 2.1e-8 native (median 1.2e-7 vs 5.6e-16) — real damage, and `-mc64 1` recovered NONE of it (identical to every printed digit); ×1e9 max 2.2e-4, again bit-for-bit no MC64 effect; ×1e12 the Jacobian reaches MA48 already structurally singular (rank 10518/10524 — the float32 `store_real` pipeline flushes small entries upstream) and MC64 itself declines by design (INFO(1)=+2 overflow-risk factors). ATTRIBUTION: bad scaling hurts TEEMS through the float32 coefficient pipeline (~1e7 relative precision, breaks first), never through the double-precision factorize (headroom ~1e12+, by which point the matrix is already destroyed upstream) — there is NO regime where solver-side scaling pays; the correct remedy for badly-scaled user data is normalization/diagnostics at the data layer. FINAL VERDICT: MC64 rejected — no performance value at scale, no reachable robustness value; shortlist item 1 closed. **MA41/MA38 multifrontal head-to-head done 2026-07-19 — REJECTED (shortlist item 2 closed)**: spec-first reads of ma41-1.2.0 (multifrontal, AMD on the A+Aᵀ pattern, repeatable JOB=2 on the kept assembly tree, BLAS-3 kernels; its own spec steers *very unsymmetric* matrices to MA38/MA48) and ma38-1.3.0 (early UMFPACK, Markowitz + dense frontal BLAS-3, MA38B/BD same-pattern refactorize); standalone single-thread double-precision bench on REAL first-step TEEMS Jacobians (dumped via an `.audit/hslvar/dump` solver variant riding the `-fastrefac` staging; explicit zeros kept; harness validated against the session-14 instrumented in-solver timings). At 1.35M×5.4M-nnz (GTAPv7 static): MA48 production path analyse 13.8s / factorize 2.87s / JOB=2 refactorize 2.75s / 25.4M factor reals / 0.63 GB; MA41 with its max-transversal remedy (ICNTL(6)=1) analyse 109s / factorize **323s (113×)** / JOB=2 **~322s (117×, no refactorize advantage)** / 432M factor reals (17× fill, 4.1 Tflop estimate) / 9.6 GB (15×); MA41 default config DNF (workspace demands exploded 93M→303M→619M+ reals with climbing off-diagonal pivots — the A+Aᵀ analysis is structurally wrong for CGE Jacobians); MA38 DNF (killed at 3600s CPU still inside its first analyse+factorize = ≥217× MA48's 16.6s; also segfaults in its own error paths at production scale — 1995-era code limits). At 8.3M×31.3M-nnz (XXL): MA48 analyse 381s / factorize 54.7s / JOB=2 ~46s / 3.6 GB (cross-checks with the observed 19-min production solves); MA41/MA38 not attempted — infeasible (MA41 ~60 GB extrapolated; int32 workspace counters would overflow) and pointless given the 1.35M margins. ATTRIBUTION: TEEMS Jacobians are extremely sparse (~4 nnz/row) and extremely unsymmetric — precisely the regime the MA41 spec itself cedes; multifrontal dense-frontal designs manufacture fill that BLAS-3 cannot buy back, while MA48's Markowitz ordering + `-fastrefac` JOB=2 keeps per-step cost unmatched. MA48 is confirmed as the right engine class. **MC33 BBT ordering evaluated 2026-07-19 — REJECTED (shortlist item 3 closed)**: spec-first read of mc33-1.0.0 (Hellerman-Rarick P⁵ ordinary / P³ nested bordered-block-triangular forms, `IFLAG(3)` = border dimension; no block-count or balance control, diagonal blocks full — a sequential spike form, not a balanced parallel partition); run on real GTAP-RE Jacobians from the same dump harness. golden-re 10.5k system: MC33 border 119 (P⁵) / 92 (P³) vs declaration-based netcut **18** (5–6.6× worse); 202k intertemporal: 2422 / 2147 vs **420** (~5× worse), with ~quadratic runtime growth (0.03s→10.2s for 19× size — hours at 4.36M, vs the declaration ordering's effectively free netcut). ATTRIBUTION: the TIME-chain semantics that the declaration/structural ordering exploits (border = lead/lag couplings only, refined to element level by E3) carry information a generic structure-only ordering cannot recover; and the "no modeler discipline needed" motivation is already served by 6.8's structural partition detection, which reproduces declaration-quality netcut from the equations alone. **HSL_MC79 evaluated 2026-07-19 — ADOPTION PROPOSED (the shortlist's first positive verdict)**: spec-first read of hsl_mc79-1.1.1 (F95, integer-only, dependency-free: `mc79_matching` = maximum matching with per-row/per-column match status, `mc79_coarse`/`mc79_fine` = Dulmage-Mendelsohn under/well/over-determined blocks + Tarjan SCCs). Probe on real Jacobians: healthy golden-re → full structural rank, all-square DM, ~5 ms; wrong-closure emulation (5 variable columns surgically emptied, system still square) → **exact identification** of all 5 defective variable elements AND the 5 over-constrained equations, plus DM localization of the entangled region, in 6 ms — the counting checks teems-R can do (squareness, classification, swap arity) are structurally blind to this class, since solvability = perfect matching on the element-level incidence graph, which needs equation semantics + data (both solver-side by design). Cost: whole-system 44 s matching + 40 s coarse at 1.35M; 2841 s matching + 2783 s coarse at 8.3M (superlinear — ~64× time for 6× rows) — but the realistic scope is block-sized (bordered methods detect singularity per diagonal block) or failure-path-only, where seconds-to-a-minute against a blind debugging session is decisive; whole-system XXL diagnosis (~1.6 h) remains acceptable as a one-off on a failed run but the integration should prefer block scope and make whole-system DM opt-in. ADOPTION SHAPE (proposed, not yet implemented): (1) MC79 ride-along on the `-solmed NoSol` structure probe — NoSol already runs data/formulas/closure, so assemble the Jacobian pattern and run the matching there: structural closure validation at deploy time with defect sets translated to variable/equation element names via the existing closure/exo_index machinery; (2) same matching on the *numerically realized* pattern (nonzero-at-base-data entries) to catch the zero-flow singularity class (structurally present, numerically undeterminable — today a mid-solve MA51 rank message with no names); (3) on-failure diagnosis in the solve paths, block-scoped where partitioned; (4) optional `mc79_fine` report: SCC sizes = the model's irreducible simultaneous cores vs recursive structure (modeler insight + condensation/backsolve guidance); (5) teems-R as presentation layer only — `ems_deploy()`-time NoSol probe surfacing `cls_err`-style named errors, no R-side TAB interpretation (single-source-of-truth rule; same rationale as retiring the E2 auto-rewrite once E3 images circulate). Limitation recorded: catches structural + zero-data classes, not numerical near-singularity (stays with MA51/condition estimates). Remaining 6.7: MA60/MC71 (diagnostics, weakened by the MC64 f32-attribution finding), KB07/08 (profile first), LA15 (parked). Bench harness kept at `.audit/hslvar/bench/` + `.audit/jacdump/` (gitignored) | user-requested 2026-07-05 |
| 6.8 | **Structural partition detection** (assessed 2026-07-06 as "block-set auto-detection"; scope deepened 2026-07-12 per user directive: the solver must be *set-agnostic* — the structure of the equation system, not set names or invariant qualifiers, determines how each set is handled, and `-regset`/`-enable_time` are slated for removal) | **done** 2026-07-12 — both ordering dimensions now derive from the equations when the transitional flags are absent: (a) *chain dimension* — `chain_refs_scan` walks the equation statements (same preprocessing as `equation_order_read`) counting lead/lag offset references per declared dimension set; `chain_set_select` aggregates to top-level sets and picks the most-referenced (the `(intertemporal)` qualifier only licenses the syntax and is cross-checked: qualified-but-never-offset-referenced sets get no chain treatment, offsets on unqualified sets warn, multi-set offsets warn and majority wins). Confirmed name-agnostic against the uom corpus (`teems_tabs/uom_based/gtap_ebm.tab` chains on `allweek`, not `alltime`; its lat/long spatial grids couple via adjacency-coefficient sums, which the probe prices as border). (b) *diagonal-block partition* (DBBD/NDBBD) — every set with ≥2 elements outside the chain family indexing a variable dimension (directly or via subset, so subregional-style sets compete) is probed: `equation_order_read` + the extracted first counting pass (`block_var_count`, now shared by probe and live ordering) measure the real border/block sizes per candidate; viable = ≥ n_tasks nonempty blocks and border < half the system; smallest border wins, 2% near-ties broken by block balance (integer-compared — every rank reaches the same answer without communication). KEY SCORING LESSON: in nested (NDBBD) mode the per-chain-block interface column must count as border — it is the local border the interface solve pays for; scoring it intra made all candidates tie at netcut 27 on GTAP-RE and balance picked `acts` (half the system in the interface), which segfaulted the solve. With the fix `reg` wins 2.9% vs `demd` 15.8% / `comm` 34.9% / `acts` 46.6%. Acceptance: NDBBD, DBBD-intertemporal, DBBD-static, SBBD golden configs rerun with the flags REMOVED reproduce their manifests bit-identically (`.audit/accept_structural.sh`); `-regset auto` alias likewise; 14/14 goldens (explicit flags) bit-identical; LU-without-flags keeps no chain treatment (bit-compat) and gets a reworded structure-ignored warning (re-gated on `alltimeset<0`, was `-enable_time`-based). stats.json v2: `chain_source`/`partition_source` (`explicit`/`structural`/`none`), `chain_set`/`partition_set`, full `partition_auto` candidate table — the structural evidence for method auto. **`-solmed NoSol` verified as the structure-probe pathway**: 0.5 s wall on the GTAP-RE golden, runs full detection, writes the complete 13-candidate table to stats.json, no solve — the concrete mechanism by which `matrix_method` auto can later consult structure before choosing a method. Method-vs-structure aborts added: SBBD without a chain dimension previously handed HSL_MP48 zero blocks and finished with exit 0 and NO solution (silent-failure footgun, unreachable from teems-R which validates enable_time) — now a clean collective abort (verdict Bcast from rank 0, since HSL ranks≠0 never resolve alltimeset). Unmatched `-regset` names now warn + fall back to detection. NDBBD/DBBD abort with remedies when required dimensions resolve to none. NB the `acts`-partition segfault is a pre-existing NDBBD fragility for pathological partitions (never selectable now, but reproducible with explicit `-regset acts`) — noted under standing investigations | feeds `matrix_method` auto (solver-side method-vs-structure validation + `NoSol` probe run pathway); **flag removal landed 2026-07-14** riding the 6.6(d) re-anchor slot: `-regset`/`-enable_time` parses removed from the solver (stale flags in old commands are ignored by the options parser) and dropped from teems-R commands; validated on the old build env for clean attribution — bordered gates bit-identical (detection reproduces the flags), LU gates' parsed solutions bit-identical (deltas confined to signed zeros + the now-unset `regional`/`intsup` sol.set flags, which teems-R parses but never consumes), nsub-lu ≤ 1.8e-9, nsub-sbbd shifts ≤ 6.7e-6 with identical accuracy distribution because its hand-written golden cmd had given SBBD a `-regset` T×R geometry teems-R never generated (flag-free ordering is strictly better: netcut 258→27); NB the `-regset acts` segfault repro in standing investigations now needs a code-level route; value-aware element-level classification (banded adjacency coefficients ≈ partitionable) pairs with 6.5/E3 |
| 6.9 | `-march` image variants | **folded into 6.6** (2026-07-09): reframed as a needed fix to the published image, not a performance item — see 6.6 for the batch |
| 6.10 | **Structure-informed `matrix_method` auto** — wire the 6.8 probe pathway into the auto decision: the 5.8 rule chooses from deploy metadata alone (system size, n_reg, n_tasks); instead run a `-solmed NoSol` probe (verified under 6.8: ~0.5 s on the GTAP-RE golden, full structural detection, complete candidate table in stats.json v2 — `chain_source`/`partition_source`, `chain_set`/`partition_set`, `partition_auto`) and let teems-R consult the measured border/block sizes and chain evidence before committing to a method. Subsumes the 5.8 residuals: the NDBBD-vs-SBBD escalation question and the 1.5–2M × <100-region band become probe-answerable instead of metadata-guessable. Probe cost must stay negligible relative to the solve (skip below a size threshold where LU is a foregone conclusion); interacts with 6.2, since condensation changes the system the probe should measure | added 2026-07-13; mechanism verified under 6.8, integration unscheduled until now |
| 6.11 | **Memory-safety program + C modernization** (added 2026-07-19, user-requested; motivated by the audit arc's leak history — Bindices, submatrix-array orphans, Euler vece, obiviindx UB, stpcpy-overlap and slack-write classes — all found *incrementally* because coverage was rig-by-rig valgrind, not because the tools were weak). **Phase A — systematic detection (harness-only, zero solver-code risk):** (a) ASan+UBSan builds (C **and** gfortran, so the cross-language handoffs are covered) + `verify-asan.sh` sweeping the full behavioral matrix — 14 goldens × 4 matsols × solmeds × `-fastrefac`/`-inmemory`/`-nsubints` — with MPI/PETSc suppression files; LSan reports everything unreachable at exit on every exercised path (~2× slowdown vs valgrind's 20–50×, which is why the full matrix becomes affordable); valgrind retained as the deep gate for uninitialized-read classes ASan can't see (slack-writes, string-overlap). (b) `gcc -fanalyzer` + clang-tidy (`clang-analyzer-*`, `bugprone-*`) + cppcheck triaged pass — targets the error-path leak class specifically. (c) TSan/Archer sweep of the OMP regions (ATOMIC accumulations, NDBBD disjoint-slot claims) — a class never yet checked; certifies or refutes the commit-message reasoning. (d) AFL++/libFuzzer on `tab_parse`/`cmf_io` under ASan (user-controlled input into fixed `TABREADLINE` buffers with hand-rolled strings = the classic hostile-input surface); soaks in background. **Phase B — code-shape changes that DELETE leak classes (each behind 14/14 + valgrind + A/B-neutrality gates):** (1) step- and solve-lifetime **arena allocators** — the found leaks were overwhelmingly per-step/per-solve scratch freed by hand far from the allocation; arenas make the class structurally impossible and collapse the `*_fastrefac_free` proliferation (PETSc objects keep destroy discipline); (2) **named-field structs for the magic-index protocols** (`INSIZE(17)`, `insize[4]`-as-status, `indata(5)` — positional-integer contracts across the C↔Fortran boundary, matching Fortran derived types or named constants); (3) `solver_ctx` **context structs** replacing globals.c + file statics + 30-parameter signatures — single-point cleanup, testability; (4) **single-exit cleanup convention** in every allocating function (the error-path class); (5) **integer hygiene**: `stdint`/`size_t` sweep with `-Wconversion` triage across the `int`/`long`/`offset_t`/`dim_t`/`PetscInt`/`fortran_int` mixture (XXL-scale int32 walls already brushed); (6) **warning burn-down** to `-Wall -Wextra` clean and enforced (current baseline: 102 lines); (7) incremental decomposition of `solve_gragg`/`main` (small functions = small ownership scopes = what makes 1–4 enforceable). NOT in scope: rewrites, wholesale PetscMalloc adoption. EFFORT (working-session estimates): Phase A ≈ 3–4 sessions total — (a) 1 session build+first sweep, +0.5–1 triage; (b) 1 session; (c) 1 session incl. LLVM/Archer container setup; (d) 1–2 sessions harness + multi-day background soak. Phase B ≈ 8–12 sessions spread out — arenas 2–3, INSIZE structs 1–2 (both C and Fortran sides), integer sweep 1–2, warnings ~1, context-structs/decomposition 4–8 as an ongoing thread behind other work. Phase A first: it answers "are there more defects right now" before any restructuring, and its harness then gates Phase B | golden bit-identity discipline is the enabler: every slice lands incrementally behind the standard gates |

## Completed phases 0–4 (2026 refactor log)

Reconstructed from git history; hashes are the anchor commits.

| phase | what was done | commits |
|---|---|---|
| 0 — verification harness | `.audit/verify.sh`: container rebuild (`teems-audit`) + golden solves checked bit-identical against manifests anchored to the pre-refactor binary; grew from 5 gates to 9 during phase 5. Benchmark rigs (`bench_run.sh` wall/RSS, `strace_run.sh` write-byte accounting, `bench-*` deployments). **Dev-machine only — not tracked in git**; described in solver-reference §12 | — |
| 1 — dead-code removal | 13 uncalled C functions + prototypes; unreachable solver-dispatch branches in `main()`; commented-out code stripped from C and Fortran; dead backup subroutine in `ha_mp48.f90`; dangling prototype; 15 uncalled Fortran kernels + externs; dead `-isLinux`/`-medthreads` options | `fdd2e76`, `08c1be8`, `4a05297`, `bab50d5`, `fa0b51b`, `b5669fe`, `7a403a5` |
| 2 — build hygiene | makefile repair (clean target, F77FLAGS typo, overridable `BUILD_DIR`/`OPT`); globals defined once, `-fcommon` dropped; PETSc private `aij.h` via include path; scratch dir configurable (`-tempdir`/`TMPDIR`); `mp48_mod.sh` sed edits replaced with committed patches; expedited build compiles current src (was stale snapshot); `BUILDING.md` added | `866fb85`, `6a958bc`, `1bfde3a`, `17f9b3a`, `74feb38`, `f775a4f`, `7a403a5` |
| 3 — de-duplication | inline Johansen and modified-midpoint blocks factored into `Johansen()`/`ModMidPoint()`; `hnew_mupdate` merged into `hnew_update` behind a midpoint flag | `15b9d5a`, `ffd7eb6`, `7a13606` |
| 4 — restructuring & renaming | `NAMING.md` conventions; types/fields/globals, functions, and source files renamed; `ha_newmfparse.c` split into `jacobian.c`/`block_order.c`/`block_solve.c`/`solve_drivers.c`; magic codes replaced with enums (`matrix_method`, `solution_method`, `bound_type`, `op_code`, `operand_type`); header documented by module section; residual `ha_*` parameters/locals/generated-name prefixes renamed | `f3ccb8e`, `fb441fd`, `92ec64c`, `b081034`, `e5a5158`, `ea9624f`, `e0f0e4c`, `4aff8be` |

Speed outcome across phases 0–4: neutral by design (golden-gated);
cleanliness and navigability were the deliverables. Performance work
began in phase 5.

## Standing investigations

- **RESOLVED 2026-07-11 — multi-subinterval bug was TWO defects** (fixed
  8778838 + the hsl_mp48d.patch revision; 14-gate golden suite green):
  (1) *xc0 unguarded free* (ours, phase 5.3 inmemory refactor,
  2026-07-05): `free(xc0)` in `solve_modified_midpoint` sat outside the
  `if(!inmemory)` guard while its reload stayed inside — under
  `-inmemory` the cumulative multiplier vanished for the sol>0
  extrapolation passes of every subinterval after the first, so the
  Richardson combination blended pass-inconsistent quantities (the
  exact 23/45 signature) and composed output violated the exogenous
  shock (pfactwld+1 → 0.5111). Symptoms varied by method/env: silent
  half-shock (LU/SBBD), a wild interface element (SBBD-2 on GTAP12a),
  bound aborts, ns=3 false singularity. Fix: free moved inside the
  guard; ns=1 outputs proven bit-identical.
  (2) *CGLOB overrun* (legacy, in the original project patch to
  HSL_MP48): the memory-reduction patch re-dimensioned
  `CGLOB(NEQ,NBLOCK)` → `CGLOB(MAXSBCOLS,NBLOCK)` with MAXSBCOLS
  estimated from block column pointers — but the border block's true
  column span (NCOL 5249 vs bound 1399 on the probe) exceeds any such
  estimate, so *every* SBBD/DBBD solve wrote ~15KB past the array
  (valgrind: invalid write, hsl_mp48d.f90:1293). Results stayed
  correct only because the overrun addresses were read back before
  reuse; ns≥2's allocation churn broke that and crashed
  (munmap_chunk) in some environments. Fix: hsl_mp48d.patch now
  measures the true per-block span in a pre-pass and sizes CGLOB from
  it; valgrind-clean; all goldens bit-identical (the round-trip had
  been output-neutral).
  Golden suite widened to 14 gates: + nsub-lu (Mmid ns=2 LU-1, run in
  BOTH -inmemory modes against one manifest — modes proven
  bit-identical), nsub-sbbd (ns=2 SBBD-2), nsub-ndbbd (ns=3 NDBBD-2),
  nsub-dbbd (static Mmid ns=2 DBBD-2); GTAP11c deployments with real
  numeraire shocks (.audit/deploy_golden_nsub.R). Coverage now spans
  solution methods × matrix methods × ranks × subintervals ×
  inmemory. teems-R test-ems_solve.R 40/40 — the "GTAP12 data class"
  cross-method failures were this bug, not the database.

## Standing investigations

- ~~`-ndbbd_bl_rank`/`-nestfile` per-block rank overrides~~ RESOLVED
  (2026-07-11): removed. Investigation corrected the semantics — "rank"
  is the NUMERICAL rank of each per-time interface block (the local
  border of the nested DBBD), not an MPI rank assignment. The options
  were a vestige of the research code's two-run workflow (`-presol`
  preparation run wrote per-block interface files recording each
  block's true rank from the rank-revealing MA51 factorization; a
  second run consumed them, with the CSV carrying the ranks between
  runs). Since the first TEEMS commit the presolve pass is hardcoded
  inline (`presol=1` at every NDBBD call site) and
  `ndbbd_order_presolve` unconditionally overwrites every array entry
  with min(nrow,ncol) before anything reads it — the CSV values could
  never influence a solve, on any code path; `-presol` was equally
  inert (parameter overwritten at all use sites; it was also read into
  an uninitialized bool when the flag was absent). The "fold into
  auto-calibration" idea is already the shipped behavior: the in-solve
  presolve measures each interface block's true rank and shrinks/
  permutes accordingly. Removed `-ndbbd_bl_rank`, `-nestfile`,
  `-presol`, `csv_read_ints()`, and the dead `./ndbbd_drank.csv`
  fallback; teems-R no longer passes `-presol 1` (compatible both
  directions — old binaries ignored the value anyway). Follow-up in the
  same sweep: `-nesteddbbd` removed too — it only ever restated
  `-matsol 3` (teems-R derived it from `matrix_method == "NDBBD"`; a
  mismatch in either direction was simply broken), so the solver now
  derives `nesteddbbd = (matsol==MM_NDBBD)`. NB solver-side this one
  is NOT ignorable by old binaries: an old solver given a new teems-R
  command runs NDBBD un-nested — runtime images must carry a solver
  at/after this change (teems:dev rebuilt).

- NDBBD-vs-SBBD crossover at scale (medium ACTS, 8–16 ranks, cluster
  hardware) — dev-box sweep found no NDBBD win region.
- NDBBD segfault under pathological partitions (found 2026-07-12 via
  6.8): `-matsol 3 -regset acts` on the GTAP-RE golden (interface
  column ≈ half the system) dies in SEGV during the solve. Structural
  detection now scores such partitions out, but the crash class
  predates 6.8 and is reachable with explicit flags; diagnose if NDBBD
  hardening becomes a priority.
- Stiffness monitoring once adaptive RK exists (see 6.3e).
- Ordering freedom in `equation_order_read_nested` (legacy note: "can
  try different varreg"); direct vs MC66 ordering netcut trade-off.
